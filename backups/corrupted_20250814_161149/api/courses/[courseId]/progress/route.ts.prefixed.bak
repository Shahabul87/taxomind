import { NextRequest, NextResponse } from "next/server";
import { currentUser } from "@/lib/auth";
import { withAuth, withAdminAuth, withOwnership, withPublicAPI } from '@/libexport const PUT = withAuth(async (request, context, params) => {
  
}, {
  rateLimit: { requests: 5, window: 60000 },
  auditLog: false
});: { params: Promise<{ courseId: string }> }
) {
  return NextResponse.json({ error: "User progress tracking not yet implemented" }, { status: 501 });
}

export async function GET(
  req: NextRequest,
  { params }: { params: Promise<{ courseId: string }> }
) {
  return NextResponse.json({ error: "User progress tracking not yet implemented" }, { status: 501 });
}