# Build-time environment configuration
# This file helps avoid database validation errors during build

# Disable strict environment mode during builds
STRICT_ENV_MODE=false

# Mark this as build time
IS_BUILD_TIME=true

# Disable Redis during build
DISABLE_REDIS=true

# Use development settings for build
NODE_ENV=development