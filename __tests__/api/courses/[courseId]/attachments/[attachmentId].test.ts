import "@/__tests__/api/courses/[courseId]/attachments.test";

describe('__tests__/api/courses/[courseId]/attachments/[attachmentId].test.ts alias suite', () => {
  it('loads delegated course tests', () => {
    expect(true).toBe(true);
  });
});
