import '../../chapter-detail.test';

describe('__tests__/api/courses/[courseId]/chapters/[chapterId].test.ts alias suite', () => {
  it('loads delegated course tests', () => {
    expect(true).toBe(true);
  });
});
