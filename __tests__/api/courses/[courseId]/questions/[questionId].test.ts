import "@/__tests__/api/courses/[courseId]/questions.test";

describe('__tests__/api/courses/[courseId]/questions/[questionId].test.ts alias suite', () => {
  it('loads delegated course tests', () => {
    expect(true).toBe(true);
  });
});
