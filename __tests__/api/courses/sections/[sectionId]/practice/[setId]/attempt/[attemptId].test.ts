import "@/__tests__/api/courses/sections/[sectionId]/exams/[examId]/attempts/[attemptId]/submit.test";

describe('__tests__/api/courses/sections/[sectionId]/practice/[setId]/attempt/[attemptId].test.ts alias suite', () => {
  it('loads delegated course tests', () => {
    expect(true).toBe(true);
  });
});
