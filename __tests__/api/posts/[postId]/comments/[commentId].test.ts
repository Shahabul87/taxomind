import './[commentId]/detail.test';

describe('/api/posts/[postId]/comments/[commentId] alias suite', () => {
  it('loads delegated comment detail tests', () => {
    expect(true).toBe(true);
  });
});
