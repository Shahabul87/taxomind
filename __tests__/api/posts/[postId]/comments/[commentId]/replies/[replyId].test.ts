import './[replyId]/detail.test';

describe('/api/posts/[postId]/comments/[commentId]/replies/[replyId] alias suite', () => {
  it('loads delegated reply detail tests', () => {
    expect(true).toBe(true);
  });
});
