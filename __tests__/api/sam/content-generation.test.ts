import "@/__tests__/api/sam/interactions.test";

describe('alias-wrapper: content-generation', () => {
  it('loads delegated suite @/__tests__/api/sam/interactions.test', () => {
    expect(true).toBe(true);
  });
});
