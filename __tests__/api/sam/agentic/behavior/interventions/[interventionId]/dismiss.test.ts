import '../dismiss.test';

describe('alias-wrapper: agentic behavior interventions [interventionId] dismiss', () => {
  it('loads delegated suite ../dismiss.test', () => {
    expect(true).toBe(true);
  });
});
