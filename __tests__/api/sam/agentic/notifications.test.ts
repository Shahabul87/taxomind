import './notifications/notifications.test';

describe('alias-wrapper: agentic notifications', () => {
  it('loads delegated suite ./notifications/notifications.test', () => {
    expect(true).toBe(true);
  });
});
