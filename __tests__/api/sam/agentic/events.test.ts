import "@/__tests__/api/sam/agentic/telemetry.test";

describe('alias-wrapper: agentic events', () => {
  it('loads delegated suite @/__tests__/api/sam/agentic/telemetry.test', () => {
    expect(true).toBe(true);
  });
});
