import '../study-plans-generate.test';

describe('alias-wrapper: study-plans generate', () => {
  it('loads delegated suite ../study-plans-generate.test', () => {
    expect(true).toBe(true);
  });
});
