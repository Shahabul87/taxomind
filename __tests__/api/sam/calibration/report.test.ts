import "@/__tests__/api/sam/competency.test";

describe('alias-wrapper: calibration report', () => {
  it('loads delegated suite @/__tests__/api/sam/competency.test', () => {
    expect(true).toBe(true);
  });
});
