import "@/__tests__/api/sam/gamification/stats.test";

describe('alias-wrapper: gamification leaderboard', () => {
  it('loads delegated suite @/__tests__/api/sam/gamification/stats.test', () => {
    expect(true).toBe(true);
  });
});
