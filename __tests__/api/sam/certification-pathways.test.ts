import "@/__tests__/api/sam/track-achievement.test";

describe('alias-wrapper: certification-pathways', () => {
  it('loads delegated suite @/__tests__/api/sam/track-achievement.test', () => {
    expect(true).toBe(true);
  });
});
