import "@/__tests__/api/sam/sessions.test";

describe('alias-wrapper: practice sessions [sessionId] end', () => {
  it('loads delegated sessions test suite', () => {
    expect(true).toBe(true);
  });
});
