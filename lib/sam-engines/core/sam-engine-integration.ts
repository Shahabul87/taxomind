/**
 * SAMEngineIntegration stub
 *
 * The original implementation was moved to lib/_deprecated/sam-engines/core/.
 * This minimal stub keeps sam-master-integration.ts compiling.
 * The class is instantiated but none of its methods are called directly
 * by SAMMasterIntegration (all engine queries go through Prisma), so
 * the stub only needs to satisfy the constructor call.
 */
export class SAMEngineIntegration {
  // No methods are invoked by sam-master-integration.ts
}
